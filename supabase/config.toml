project_id = "empi-network"

[functions.discord-admin-api]
verify_jwt = true

[functions.network-admin-api]
verify_jwt = true

# Los formularios pueden ser usados por visitantes sin sesión. La función
# aplica su propia validación de esquema, honeypot, idempotencia y rate limit.
[functions.network-public-api]
verify_jwt = false
